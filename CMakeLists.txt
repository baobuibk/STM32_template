cmake_minimum_required(VERSION 3.20)
set (PROJECT_NAME             "STM32_Cmake")
set (PROJECT_UNIT_TEST_WINDOWS  "uinit-test-windows")
project(${PROJECT_NAME})

if (BUILD_FIRMWARE STREQUAL "ON")
    enable_language(C ASM)  
    include(${CMAKE_CURRENT_SOURCE_DIR}/cmake/build_firmware.cmake)
elseif (BUILD_HOST_TESTS STREQUAL "ON")
    enable_language(C)  
    include(${CMAKE_CURRENT_SOURCE_DIR}/cmake/build_unit_test_windows.cmake)
endif()
